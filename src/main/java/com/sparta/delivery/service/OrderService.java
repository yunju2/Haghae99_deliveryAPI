package com.sparta.delivery.service;

/*
import com.sparta.delivery.dto.order.responsedto.OrderDto;
import com.sparta.delivery.model.Restaurant;
import com.sparta.delivery.repository.FoodRepository;
import com.sparta.delivery.repository.OrderRequestRepository;
import com.sparta.delivery.repository.OrdersRepository;
import com.sparta.delivery.repository.RestaurantRepository;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;

import javax.transaction.Transactional;
import java.util.List;

@Service
@RequiredArgsConstructor
public class OrderService {
    private final OrdersRepository ordersRepository;
    private final RestaurantRepository restaurantRepository;
    private final FoodRepository foodRepository;
    private final OrderRequestRepository orderRequestRepository;

    @Transactional
    public List<OrderDto> getAllOrders(){

    }

    @Transactional
    public OrderDto order (OrderDto orderDto){
        Restaurant restaurant = restaurantRepository.findById(orderDto.get);
    }
}
*/
